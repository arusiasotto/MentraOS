{
  "name": "watch-gestures-miniapp",
  "private": true,
  "description": "On-device swipe and tap tester for Mentra wearables",
  "scripts": {
    "build": "bun run build.ts",
    "pack": "bun run build && mkdir -p build && cd dist && zip -qr ../build/com.mentra.watchgestures-$(node -p \"require('../miniapp.json').version\").zip ."
  },
  "dependencies": {
    "@mentra/miniapp": "workspace:*"
  },
  "devDependencies": {
    "@mentra/miniapp-cli": "workspace:*",
    "bun-types": "^1.3.14"
  }
}
